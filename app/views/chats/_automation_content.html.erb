<div class="container" id="<%= dom_id(automation) %>">
  <% if automation %>
    <p><%= "Title : #{automation.title}" %></p>
    <p><%= "Description : #{automation.description}" %></p>
    <p><%= "Content : #{automation.content}" %></p>
  <% end %>
</div>
